<section class="hero">
    <div class="image-wrapper"><img src="images/background.jpg" alt=""></div>
    <div class="hero-wrapper">
        <div class="text-wrapper">
            <h1>Laptops y soporte inteligente para ti</h1>
            <h2>Monitoreo preventivo, soporte rápido y accesorios confiables en un solo lugar</h2>
        </div>
        <div class="call-to-action">
            <a href="/products">Ver planes</a>
        </div>
    </div>
</section>

<style>
    .hero {
        position: relative;
        width: 100%;
        aspect-ratio: 16/9;
    }

    .image-wrapper {
        position: absolute;
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        height: 100%;
    }

    .image-wrapper > img {
        mix-blend-mode: lighten;
        width: 200%;
        opacity: .9;
    }

    .hero-wrapper {
        position: absolute;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: space-evenly;
        align-items: center;
        /* gap: 8rem; */
        text-align: center;
        background: radial-gradient(circle, var(--primary-color-transparent), transparent, transparent);
    }
    
    .text-wrapper {
        display: flex;
        flex-direction: column;
        gap: 3rem;
    }

    h1 {
        color: var(--neutral-color);
        text-shadow: 0rem 0rem 10rem var(--primary-color);
        font-size: 8rem;
        text-wrap: balance;
    }

    h2 {
        color: var(--neutral-color);
        text-shadow: 0rem 0rem 10rem var(--primary-color);
        font-size: 3rem;
        text-wrap: balance;
    }

    .call-to-action {
        transition-duration: .2s;
    }

    .call-to-action > a {
        padding-inline: 4rem;
        padding-block: 2rem;
        font-size: 3rem;
        background-color: var(--secondary-color);
        border-radius: .5rem;
        color: var(--neutral-color);
        box-shadow: 0rem 0rem 5rem var(--primary-color);
        font-weight: bold;
    }

    .call-to-action:hover {
        scale: 1.1;
    }
</style>